interface:
  display_name: "Roslyn Conditional Matrix"
  short_description: "Compare C# configurations, frameworks, and symbols"
  default_prompt: "Use $csharp-roslyn-conditional-compilation-matrix to perform the requested compiler-accurate C# analysis."
dependencies:
  tools:
    - type: "mcp"
      value: "csharp_roslyn"
      description: "Local read-only Roslyn code-intelligence MCP server"
