interface:
  display_name: "Roslyn Test Impact"
  short_description: "Find tests statically reachable from a C# symbol"
  default_prompt: "Use $csharp-roslyn-test-impact to perform the requested compiler-accurate C# analysis."
dependencies:
  tools:
    - type: "mcp"
      value: "csharp_roslyn"
      description: "Local read-only Roslyn code-intelligence MCP server"
