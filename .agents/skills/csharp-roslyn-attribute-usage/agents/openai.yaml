interface:
  display_name: "Roslyn Attribute Usage"
  short_description: "Find attribute applications and migration metadata"
  default_prompt: "Use $csharp-roslyn-attribute-usage to perform the requested compiler-accurate C# analysis."
dependencies:
  tools:
    - type: "mcp"
      value: "csharp_roslyn"
      description: "Local read-only Roslyn code-intelligence MCP server"
