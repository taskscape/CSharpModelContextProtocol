interface:
  display_name: "Roslyn Region Flow"
  short_description: "Analyze data and control flow in a source region"
  default_prompt: "Use $csharp-roslyn-region-flow to perform the requested compiler-accurate C# analysis."
dependencies:
  tools:
    - type: "mcp"
      value: "csharp_roslyn"
      description: "Local read-only Roslyn code-intelligence MCP server"
