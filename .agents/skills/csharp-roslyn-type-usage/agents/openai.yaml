interface:
  display_name: "Roslyn Type Usage"
  short_description: "Classify construction, DI, and API type uses"
  default_prompt: "Use $csharp-roslyn-type-usage to perform the requested compiler-accurate C# analysis."
dependencies:
  tools:
    - type: "mcp"
      value: "csharp_roslyn"
      description: "Local read-only Roslyn code-intelligence MCP server"
