interface:
  display_name: "Roslyn Find References"
  short_description: "Classify exact compiler-bound symbol references"
  default_prompt: "Use $csharp-roslyn-find-references to perform the requested compiler-accurate C# analysis."
dependencies:
  tools:
    - type: "mcp"
      value: "csharp_roslyn"
      description: "Local read-only Roslyn code-intelligence MCP server"
