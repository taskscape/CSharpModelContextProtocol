interface:
  display_name: "Roslyn Context Bundle"
  short_description: "Gather a bounded goal-specific semantic context"
  default_prompt: "Use $csharp-roslyn-context-bundle to perform the requested compiler-accurate C# analysis."
dependencies:
  tools:
    - type: "mcp"
      value: "csharp_roslyn"
      description: "Local read-only Roslyn code-intelligence MCP server"
