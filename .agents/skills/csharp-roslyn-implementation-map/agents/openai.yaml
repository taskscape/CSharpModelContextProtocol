interface:
  display_name: "Roslyn Implementation Map"
  short_description: "Map C# contracts to implementations and overrides"
  default_prompt: "Use $csharp-roslyn-implementation-map to perform the requested compiler-accurate C# analysis."
dependencies:
  tools:
    - type: "mcp"
      value: "csharp_roslyn"
      description: "Local read-only Roslyn code-intelligence MCP server"
