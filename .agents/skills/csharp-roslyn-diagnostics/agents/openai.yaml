interface:
  display_name: "Roslyn Diagnostics"
  short_description: "Run scoped compiler and analyzer diagnostics"
  default_prompt: "Use $csharp-roslyn-diagnostics to perform the requested compiler-accurate C# analysis."
dependencies:
  tools:
    - type: "mcp"
      value: "csharp_roslyn"
      description: "Local read-only Roslyn code-intelligence MCP server"
