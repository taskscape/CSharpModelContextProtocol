interface:
  display_name: "Roslyn Project Dependencies"
  short_description: "Inspect project, package, and namespace dependencies"
  default_prompt: "Use $csharp-roslyn-project-dependencies to perform the requested compiler-accurate C# analysis."
dependencies:
  tools:
    - type: "mcp"
      value: "csharp_roslyn"
      description: "Local read-only Roslyn code-intelligence MCP server"
